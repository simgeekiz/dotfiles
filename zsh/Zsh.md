- zprofile
  PATH configuration

- config.zsh
  environment variables
  shell options
  LS_COLORS configuration

- completion.zsh
  compinit
  zstyle completion settings

- key-bindings.zsh
  bindkey commands

- zsh_aliases.zsh
  aliases like ls, ll, grep

- prompt.zsh
  prompt functions and PROMPT
